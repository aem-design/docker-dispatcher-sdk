#!/bin/sh

#
# Specify default environment type if none set
#
if [ -z "${ENVIRONMENT_TYPE}" ]; then
  ENVIRONMENT_TYPE=dev
else
  echo "Detected environment type: ${ENVIRONMENT_TYPE}"
fi

cat >> /usr/sbin/envvars <<EOF

# Generated by $0
export ENVIRONMENT_TYPE="${ENVIRONMENT_TYPE}"
EOF
