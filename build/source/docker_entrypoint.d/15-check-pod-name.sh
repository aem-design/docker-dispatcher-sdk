#!/bin/sh

#
# Specify dummy pod name for metadata vhost if none set
#
if [ -z "${POD_NAME}" ]; then
  POD_NAME=dummypodname
else
  echo "Detected pod name: ${POD_NAME}"
fi

cat >> /usr/sbin/envvars <<EOF

# Generated by $0
export POD_NAME="${POD_NAME}"
EOF
